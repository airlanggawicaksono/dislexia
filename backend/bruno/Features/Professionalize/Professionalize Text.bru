meta {
  name: Professionalize Text
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/v1/{{access_hash}}/professionalize/process
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "text": "i think we shoud meet up next week to talk bout the project stuff and things"
  }
}

assert {
  res.status: eq 200
  res.body.feature: eq professionalize
}
