meta {
  name: Define - History
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/me/define/history
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.feature: eq define
  res.body.items: isArray
}
