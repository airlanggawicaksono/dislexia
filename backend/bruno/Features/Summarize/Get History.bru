meta {
  name: Summarize - History
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/me/summarize/history
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.feature: eq summarize
  res.body.items: isArray
}
